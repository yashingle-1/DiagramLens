# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.1
 
# Settings & validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database
sqlalchemy>=2.0.36
asyncpg>=0.30.0          # async PostgreSQL driver
alembic>=1.13.1          # database migrations

# Redis
redis>=5.0.4
hiredis>=2.3.2           # faster redis parsing
 
# LLM Providers
google-generativeai>=0.8.0    # Gemini
anthropic>=0.40.0             # Claude (optional, benchmarking)
openai>=1.109.1               # GPT-4V (optional, benchmarking)
 
# File handling
python-multipart>=0.0.18  # for file uploads
pillow>=10.4.0           # image validation
 
# Classical CV pipeline (DiagramLens research contribution)
# contrib build, pinned below 5.x: LSD (cv2.createLineSegmentDetector) is used
# by the hybrid connection detector, and OpenCV 5 is a major API bump this
# project has not been validated against. Install ONE opencv distribution only
# — opencv-python and opencv-contrib-python both provide `cv2` and shadow
# each other.
opencv-contrib-python>=4.10,<5
pytesseract==0.3.13

# Hybrid pipeline v2 — PaddleOCR PP-OCRv5 text engine (proposer P3).
# paddlex is pinned: 3.7 changed PaddlePredictorOption's signature and breaks
# paddleocr 3.1 at construction time ("takes 1 positional argument but 2 were
# given"), which silently drops the arm back to the Tesseract fallback.
paddlepaddle==3.0.0
paddleocr==3.1.0
paddlex==3.1.0

# torch/transformers serve BOTH arms: CLIP powers icon-bank retrieval in v2,
# and segment-anything is only needed for the HYBRID_VERSION=v1 ablation.
# torch CPU build: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision
transformers>=4.35.0
segment-anything
# SAM checkpoint (375MB, not in pip): download sam_vit_b_01ec64.pth from
# https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth
# into backend/models/sam/ (path configurable via SAM_CHECKPOINT env var)

# Utilities
python-dotenv>=1.0.1
httpx>=0.27.0
tenacity>=8.3.0          # retry logic
 